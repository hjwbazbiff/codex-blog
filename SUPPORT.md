# Support

Open issues in `https://github.com/hjwbazbiff/codex-blog/issues`. Include the command, wrapper JSON output when available, Python version, and whether optional provider credentials were configured.
